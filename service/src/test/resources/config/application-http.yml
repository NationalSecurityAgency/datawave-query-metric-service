spring:
  security:
    datawave:
      use-trusted-subject-headers: true
      jwt:
        enabled: true

server:
  ssl:
    enabled: false

datawave:
  query:
    metric:
      client:
        transport: http
